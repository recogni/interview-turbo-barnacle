#include <iostream>

#include "matrix.h"

int main(const int argc, const char** argv)
{
    /*
     *  TODO (5) : Get the following to run:
     */
    // Matrix<int, 4, 4> m44 = {};
    // std::cout << m44.Get(0, 3) << std::endl;
    // std::cout << m44.GetRow(0) << std::endl;
    // std::cout << m44.GetCol(1) << std::endl;

    /*
     *  TODO (6)
     */
    // for (int i = 0; i < 4; ++i)
    //     for (int j = 0; j < 4; ++j)
    //         m44.Set(i, j, i * j);

    /*
     *  TODO (7)
     */
    // Matrix<int, 4, 2> m42 = {};
    // for (int i = 0; i < 4; ++i)
    //     for (int j = 0; j < 2; ++j)
    //         m42.Set(i, j, i - j);
    // auto prod = m44 * m42;

    /*
     *  TODO (8) :: Remove me!!
     */
    std::cout << "Edit `main.cpp` after you have finished the TODOs "
              << "in matrix.h" << std::endl;
}
