#include <stdio.h>
#include <assert.h>

#include "setbit.h"

int main(const int argc, const char** argv)
{
    /*
     *  TODO (2) : Get the following to run:
     */

    //assert(getbit_in_byte(0x07, 2) == 1);
    //assert(getbit_in_byte(0x07, 3) == 0);
    //assert(getbit_in_byte(0x07, 6) == 0);

    /*
     *  TODO (4) : Get the following to run:
     */

    //uint8_t testbyte = 0x00;
    //setbit_in_byte(&testbyte, 4, 1);
    //assert(testbyte == 0x10);

    //testbyte = 0xFF;
    //setbit_in_byte(&testbyte, 3, 0);
    //assert(testbyte == 0xf7);


    /*
     *  TODO (5) :: Remove me!!
     */
    printf("Edit setbit.h and main.cpp to implement and test each function");
}
